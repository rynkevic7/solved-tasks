/*
No Story

No Description

Only by Thinking and Testing

Look at result of testcase, guess the code!
 */

const testit = s => s.split(" ").map(i => i.slice(0, -1) + i.slice(-1).toUpperCase()).join(" ");